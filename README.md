# ASA-Dodgers

## Frontend

Pitch analysis UI (Vite + React). See [frontend/README.md](frontend/README.md) for setup.

```bash
cd frontend
npm install
npm run dev
```

Requires the ReleaseFrame analysis API (typically `http://localhost:8000`).
