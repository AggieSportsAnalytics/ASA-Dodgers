# Copy to .env.local for local development.
#
# Default: call the ReleaseFrame API at http://localhost:8000 (set API URL in the app UI).
#
# Optional ngrok proxy (avoids Safari/CORS with free ngrok):
# VITE_RELEASEFRAME_USE_PROXY=1
# VITE_PROXY_API_TARGET=https://YOUR-SUBDOMAIN.ngrok-free.dev
